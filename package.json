{
  "name": "game-store-library",
  "version": "1.0.0",
  "description": "A game store library application with RAWG and IGDB integration",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "cd server && node igdb-proxy.js",
    "start": "node server/igdb-proxy.js",
    "render-build": "npm install && cd server && npm install && cd .. && npm run build"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  },
  "dependencies": {
    "concurrently": "^8.0.0"
  }
}
